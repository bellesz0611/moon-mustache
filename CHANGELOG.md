# Changelog

## 0.1.0

- initial MoonBit Mustache rendering core
- template reference scanning APIs and CLI scan mode
- escaped and unescaped interpolation
- sections, inverted sections, comments, partials, delimiter changes
- JSON rendering helpers and checked render options
- bundle rendering APIs, manifest profiles, validation, and generation plans
- CLI support for single-template rendering and bundle workflows
- official `mustache/spec` fixture import and report generation
- `moon info` / publish dry-run workflow preparation
- showcase, scaffold, scenario, benchmark, and downstream consumer demos
