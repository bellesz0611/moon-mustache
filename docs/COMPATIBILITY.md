# Compatibility Matrix

This document tracks the current compatibility posture of Moon Mustache against common Mustache expectations.

Current status summary:

- 47 spec-style cases organized into 9 suites
- 136 imported official `mustache/spec` fixture cases
- about `7459` MoonBit lines across library, CLI, demos, reports, benchmarks, consumer demos, bridge code, and companion blueprint proof
- Markdown compatibility report generated by `moon run spec_report`
- official fixture compatibility report generated by `moon run official_spec_report`
- scenario-level workflow report generated by `moon run scenario_report`
- validation and plan reports available for bundle manifests
- 64 automated tests passing locally
- independent playground smoke workflow validates the browser-facing demo bridge

## Supported

- escaped interpolation
- unescaped interpolation
- dotted name lookup
- implicit iterator via `{{.}}`
- sections
- inverted sections
- comments
- partials
- recursive partial expansion
- delimiter changes
- standalone line trimming for section-like tags
- parent-scope lookup inside nested sections
- JSON-driven rendering helpers
- whitespace-sensitive inline behavior checks
- diagnostics coverage for malformed templates and partials

## Supported with diagnostics

- malformed template structure
- malformed partial templates
- missing partials
- partial recursion depth overflow
- missing variables when strict render options are enabled

## Available in CLI

- inline template rendering
- inline key/value context input
- dotted path key/value context input such as `user.name=Alice`
- inline JSON context input
- Base64 JSON context input
- template / context / partial loading from files on `js` target
- output-to-file flow on `js` target
- manifest-driven multi-file generation on `js` target
- bundle profile selection on `js` target
- bundle check-only validation mode
- bundle validation and generation plan report export
- strict missing-variable reporting

## Reportable suites

- interpolation
- comments
- sections
- inverted sections
- partials
- delimiters
- standalone
- whitespace
- diagnostics

## Official fixture status

- official imported cases passing: `136`
- official imported cases skipped: `0`

## Not yet complete

- target-agnostic library-level file loading
- fully automated upstream fixture synchronization in CI
- compatibility comparison against multiple existing Mustache implementations
